import React, {useEffect} from 'react';
import {View} from 'react-native';
import {useTheme} from '@configs';
import styles from './styles';
export default function SignOTP({navigation}) {
  const {colors} = useTheme();
  useEffect(() => {}, []);
  return <View></View>;
}
