import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  bannerContainer: {
    position: 'absolute',
    width: '100%',
  },
  actionContainer: {
    flex: 1,
    paddingHorizontal: 16,
  },
});
