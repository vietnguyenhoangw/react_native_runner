import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  bannerContainer: {
    position: 'absolute',
    width: '100%',
    padding: 1,
    paddingHorizontal: 16,
  },
  actionContainer: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    paddingHorizontal: 16,
  },
});
